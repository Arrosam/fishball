<?xml version="1.0" encoding="utf-8"?>
<!--
  Solid ink, like the masthead.

  It used to be the concrete grid, which at icon size read as a dark angular shape with a pink
  eye behind bars - menacing rather than friendly. The band at the top of every screen is ink
  with a paper fish on it and has never looked like that, so the icon is now the band.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#101010"
        android:pathData="M0,0h108v108h-108z" />
</vector>
